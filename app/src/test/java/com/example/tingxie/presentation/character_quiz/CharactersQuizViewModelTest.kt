package com.example.tingxie.presentation.character_quiz

import com.example.tingxie.domain.use_case.CharacterUseCases
import org.junit.Assert.*
import org.junit.Test

class CharactersQuizViewModelTest {
    @Test
    fun `when CharacterQuizViewModel is given an array of size 0 it doesn't crash, no exception thrown`() {
        val mockCharacterUseCases = Mockito.mock(CharacterUseCases::class.java)

    }
}